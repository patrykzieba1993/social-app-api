const POST_TABLE_NAME = require('../const/tableNames').POSTS;

module.exports = {
  up: (queryInterface, Sequelize) => {
    const PostsTable = {
      id: {
        allowNull: false,
        autoIncrement: true,
        primaryKey: true,
        type: Sequelize.INTEGER,
      },
      userId: {
        type: Sequelize.INTEGER,
        references: {
          model: 'Users',
          key: 'id',
        },
      },
      content: {
        type: Sequelize.STRING,
        allowNull: false,
      },
      createdAt: {
        allowNull: false,
        type: Sequelize.DATE,
      },
      updatedAt: {
        allowNull: false,
        type: Sequelize.DATE,
      },
      deletedAt: {
        allowNull: true,
        type: Sequelize.DATE,
      },
    };
    return queryInterface.createTable(POST_TABLE_NAME, PostsTable);
  },

  down: (queryInterface) =>
    queryInterface.dropTable(POST_TABLE_NAME),
};
