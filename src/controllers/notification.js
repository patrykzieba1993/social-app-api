const RouteController = require('./route');

class NotificationController extends RouteController {
  createPostNotification(request, reply) {
    
  }
  
  createCommentNotification(request, reply) {
    
  }
}

module.exports = NotificationController;